# MLflow PostgreSQL Database Configuration
# Copy this file to .env and customize the values for your environment

# PostgreSQL credentials (change these for production/shared environments)
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow
POSTGRES_DB=mlflow

# Ray Redis password (change for production)
RAY_REDIS_PASSWORD=ray
